export enum UserRole {
  Admin = 'admin',
  Agent = 'agent',
  Reporter = 'reporter',
}

export enum TicketPriority {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

export enum TicketStatus {
  New = 'new',
  InProgress = 'in_progress',
  Resolved = 'resolved',
}
