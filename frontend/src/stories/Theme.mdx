import { Meta } from '@storybook/angular';
import { Title, Subtitle, Description, ColorPalette, ColorItem, Heading } from '@storybook/blocks';
import { themeColors } from './theme-tokens';

export const meta = {
  title: 'Design System/Theme',
  tags: ['docs'],
};

<Meta of={meta} />

<Title>Helpdesk Design Theme</Title>
<Subtitle>Colors, typography, and reusable styling decisions that shape the Helpdesk UI.</Subtitle>
<Description>
  Use this guide to preview the foundational palette and baseline typography. Keep the Sass tokens in `_theme.scss`
  aligned with this reference so Storybook stories match the live application.
</Description>

<Heading level={2}>Color Palette</Heading>

<ColorPalette>
  {themeColors.map(({ title, token, cssVar, hex, description }) => (
    <ColorItem
      key={title}
      title={title}
      subtitle={`${token} / ${cssVar}`}
      colors={[{ name: 'Base', value: hex, description }]}
    />
  ))}
</ColorPalette>

<Heading level={2}>Typography</Heading>

<Description>
  The application uses the default Angular Material typographic scale mapped to the Roboto font stack. Use the hierarchy
  below when composing cards, tables, and detail views.
</Description>

<section>
  <h1>Heading 1 – Page title</h1>
  <p>Use sparingly for main views like the dashboard or ticket list.</p>

  <h2>Heading 2 – Section title</h2>
  <p>Great for card headers and grouped information inside detail views.</p>

  <h3>Heading 3 – Subsection</h3>
  <p>Helpful when you need one more layer of hierarchy within cards or panels.</p>

  <p>
    Body text defaults to 16px with a 1.5 line-height. Use it for descriptions, comments, and supporting
    context. Keep paragraphs short for readability.
  </p>

  <small>Caption text communicates metadata like timestamps or helper labels.</small>
</section>
